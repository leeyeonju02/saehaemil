import { Typography } from "@mui/material";
import { PageSection } from "@/components/ui";

export default function BoardSection() {
  return (
    <PageSection>
      <Typography variant="body1" paragraph>
        소통 게시판 내용을 여기에 작성하세요.
      </Typography>
    </PageSection>
  );
}
